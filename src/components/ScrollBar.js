import React from "react";
import PageProgress from "react-page-progress";

export const ScrollBar = () => {
  return (
    <div>
      <PageProgress height={3} color={"rgb(247, 75, 75)"} />
    </div>
  );
};
